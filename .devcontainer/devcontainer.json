{
  "name": "Playwright Codespace",
  "image": "mcr.microsoft.com/playwright:v1.41.0-jammy",
  "postCreateCommand": "npm install && npx playwright install --with-deps",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-playwright.playwright"
      ]
    }
  }
}