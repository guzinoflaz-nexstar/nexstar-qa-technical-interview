/**
 * Test requirements:

    Create a test suite that defines a SITE_URL variable set to: https://www.theguardian.com/international
    Add a setup hook that verifies the site responds with a 200 status code.
    Add a test case that clicks the site logo in the navigation bar.
    Assert the value of SITE_URL matches the current URL.
    Add a teardown hook that takes a screenshot.
    The test must have the tag @test_hooks
 */

import { test, expect } from '@playwright/test';

test.describe('<replace_this_with_your_title>', () => {
  // Candidate's solution goes here
});
